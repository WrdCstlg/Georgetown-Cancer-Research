# AfriCAN DANCE — Definitions the analysis needs from you

**For:** Dr. Laura · **From:** the computational team · **Purpose:** a short set of domain definitions that the AI/analysis layer needs pinned before it can run on real data.

The tools are built so they **cannot invent these values** — they stop and ask rather than guess. Where the grant already states a threshold, we've pre-filled it below so you can simply confirm (or adjust). The second set are genuinely open — only you can set them. Anywhere you're unsure, write *"field default, flag for review"* and we'll proceed conservatively (results marked provisional) until you confirm.

You can reply inline in this document.

---

## Part A — Confirm the grant's thresholds (quick)

Do these hold for the ancestry-aware analysis, or would you adjust any?

| # | Threshold (from the grant) | Confirm / adjust |
|---|----------------------------|------------------|
| A1 | Driver gene significance: FDR **q < 0.05**, support from ≥2 methods; COSMIC CGC genes relaxed to **q < 0.25** | |
| A2 | Differential expression: **p < 0.05** and **|log₂FC| > 0.5** | |
| A3 | Germline filter for somatic calls: population MAF **< 1%** (ExAC / gnomAD / 1000G) | |
| A4 | Minimum sequencing depth: **20×** | |
| A5 | MSI status: MSISensor2 **≥ 3.5 = MSI-H**, else MSS | |
| A6 | Tumor mutation burden: **≥ 10 mut/Mb = TMB-high** | |
| A7 | Discrete AFR strata (if used): **Low ≤30% · Medium 30–80% · High ≥80%** — note we default to *continuous* AFR proportion; is a discrete cut ever needed, and if so are these the right bins? | |
| A8 | VUS reclassification consensus rule (currently a placeholder, written during the Phase-2 build): a variant is called **pathogenic if ≥2 of the 4 tools** (AlphaMissense, EVE, PolyPhen, SIFT) call pathogenic and none call benign; **benign if ≥2 call benign** and none call pathogenic; **otherwise it stays VUS**. Confirm or adjust — including whether all four tools should count equally.<br><br>**Two tools are now wired, so this is no longer abstract.** On our 20-variant test set the rule takes VUS from 20/20 down to 13/20. Of the 13 that remain, **7 are blocked purely because the two tools disagree** — not because data is missing. These are the actual cases you would be ruling on:<br><br>`KRAS G12D` · AlphaMissense **pathogenic**, EVE *uncertain*<br>`KRAS G13D` · AlphaMissense **pathogenic**, EVE *uncertain*<br>`KRAS A146T` · AlphaMissense **pathogenic**, EVE *uncertain*<br>`TP53 R175H` · AlphaMissense **pathogenic**, EVE *uncertain*<br>`TP53 R248Q` · AlphaMissense **pathogenic**, EVE *uncertain*<br>`PIK3CA E545K` · AlphaMissense **pathogenic**, EVE *uncertain*<br>`PIK3CA H1047R` · AlphaMissense *uncertain*, EVE **benign**<br><br>Six of the seven are "one tool is confident, the other declines to commit" rather than a true contradiction. *What we need:* should a tool saying **Uncertain** count as a vote against, or as an **abstention** (so one confident call plus one abstention could suffice)? Should the tools be weighted rather than counted equally? We have deliberately changed nothing about the rule — this is your call. See decision D-010 | |
| A9 | **AlphaMissense cut-points.** AlphaMissense is now wired to its real published scores. Its publisher splits the 0–1 score at **< 0.34 = likely benign**, **> 0.564 = likely pathogenic**, **in between = ambiguous**. We transcribed those numbers exactly as published and have **not** altered them. Two things to confirm: (i) are the publisher's cut-points the right operating point for this study, or would you set them differently (a stricter pathogenic bar, a wider ambiguous band)? (ii) the publisher chose this split to reach roughly 90% precision **on a general population** — is that the right target here? *Worked example of why this matters:* PIK3CA **H1047R**, a textbook oncogenic hotspot, scores just under the 0.564 line — so AlphaMissense reports it **ambiguous**, not pathogenic. Our tooling reports what the tool says, not what the literature expects; whether and how to close that gap is your call | |
| A10 | **Is "pathogenic" the same question as "driver"?** AlphaMissense answers *is this variant clinically pathogenic* — broadly a germline/Mendelian question. Phase 2 uses it to help reclassify **somatic** variants, where what matters is whether a variant **drives** the tumor. Activating hotspots are common in tumors because they confer growth advantage, not because they are rare damaging alleles, so the two questions can come apart. **We tested this rather than assuming it** (`docs/probes/alphamissense-driver-coverage.md`): across 178 recurrent hotspots in your 15 named CRC driver genes, AlphaMissense called 93.5% of *activating* and 95.0% of *loss-of-function* hotspots pathogenic — **no significant difference** (p = 0.31 / 0.75). So the worry does **not** generalise. But two things remain: (i) PIK3CA **H1047R** ("ambiguous") is a real miss, and because it is so common it carries ~96% of the missed activating tumor burden; (ii) all five activating misses were PIK3CA specifically. **What we need from you:** should variant-effect reclassification stay a *pathogenicity* axis (and be reported as such), or should somatic **driver** evidence — hotspot recurrence, COSMIC, or the planned IntOGen step — also count toward calling a variant reclassified? See decision D-008; we have recommended but not chosen.<br><br>**Update since EVE was wired:** H1047R now has a second, independent read and it points the same way — EVE calls it **benign**, further from pathogenic than AlphaMissense's "ambiguous". Notably, EVE disagrees with the **ClinVar record it ships itself**, which says Pathogenic. Two independent models declining to call the most common activating hotspot in colorectal cancer is worth your attention even though the general pattern did not hold | |
| A11 | **EVE's class assignment.** EVE is now wired to its real published data. EVE does not publish a simple score threshold; instead it **keeps the 75% most confident classifications and labels the remaining 25% "Uncertain"**. We transcribed that scheme exactly and have **not** altered it. Two things to confirm: (i) is 75%-retained the right operating point for this study, or would you want a wider/narrower uncertain band? (ii) EVE is an **evolutionary** model — it infers damage from conservation across species — whereas AlphaMissense is largely structural. That difference is *why* the two disagree on the hotspots listed in A8. Is EVE's evidence type one you want weighted equally with AlphaMissense's for somatic variants, or differently? *Note:* EVE covers 13 of your 15 named CRC driver genes — it does **not** publish **RNF43** or **FBXW7**. RNF43 matters here specifically because your own preliminary data shows its mutation frequency varying significantly by population (p = 0.0047), highest in NHW at 73.6% — "nearly double that seen in the other populations". See decisions D-009 and D-010 | |
| A12 | **Does "calibration" even apply to an evidence lookup, rather than a prediction?** AlphaMissense and EVE are predictors trained largely on European-ancestry data, so stamping their output *calibration pending* is straightforward. The new drivers step is different in kind: it does not predict anything, it **looks up** whether a variant's residue falls in a region IntOGen found significantly mutated in published cohorts. We have stamped it `calibration_pending` too, for this reason: IntOGen's significant regions are significant **relative to the mutation spectrum of the cohorts they were computed on** (CPTAC, Hartwig, TCGA — overwhelmingly European ancestry). The consequence is asymmetric. **Presence** of evidence is fairly transferable — a residue recurrently mutated in colorectal tumours is a fact about the protein. **Absence is uninformative rather than negative**: a residue may fail to reach significance because those cohorts had no power to detect recurrence at that position *in that population*, not because it is unimportant. Recurrence-based significance is partly a statement about who was sequenced. *What we need:* is that the right treatment? Specifically — should a downstream reader ever be allowed to treat "no driver evidence" as evidence **against** a variant? We have built it so they cannot, because doing so would convert a sampling artifact into a negative finding in the direction that most disadvantages the populations this study is about | |
| A13 | **Should driver evidence be colorectal-specific or pan-cancer?** This changes real answers, so we have not chosen. Example, measured: **PIK3CA H1047R** — the hotspot that AlphaMissense called *uncertain* and EVE called *benign* — falls inside an IntOGen significant cluster in **35 of 109 pan-cancer** cohort rows and in **0 of 2 colorectal (COAD/READ)** rows. The colorectal clusters IntOGen publishes for PIK3CA are residues 542–546 (helical domain); 1047 (kinase domain) is not among them. So under colorectal scope H1047R has no driver support; under pan-cancer scope it has strong support largely contributed by breast tumours. *What we need:* for this study, should driver evidence be restricted to colorectal cohorts (fewer, smaller, but on-target), or drawn pan-cancer (far more evidence, but importing tumour types this study is not about)? We default to colorectal and record the scope on every result, so the choice is visible and reversible | |
| A14 | **IntOGen inclusion criteria.** The drivers step consumes rows IntOGen already filtered at its own published significance settings — driver methods at q < 0.1, smRegions domains q < 0.1, OncodriveCLUSTL linear clusters p < 0.05, HotMAPS 3D clusters q < 0.05. We transcribed these exactly and have **not** re-filtered or adjusted them. *What we need:* confirm these are acceptable, or tell us a stricter bar. Note also that IntOGen has **no colorectal rows at all** for 5 of your 15 named driver genes — MLH1, MSH2, MSH6, PMS2, TGFBR2 — consistent with their drivers being truncating or germline rather than clustered missense. Those variants get "no evidence in scope", which is **not** the same as "not a driver" | |

---

## Part B — Definitions only you can set (the ones currently blocking)

These aren't in the grant and the analysis can't proceed to conclusions without them.

**B1 · When is a variant-effect tool trustworthy for a given population?**
The tools we use to reclassify "unknown" variants (AlphaMissense, EVE, and the older SIFT/PolyPhen) were trained mostly on European-ancestry data. Until we know a tool is validated for African American, Ghanaian, and Ethiopian tumors, every call it makes is currently stamped *"calibration pending"* and shown **with that caveat rather than as a confident result**.
*What we need:* is there a benchmark or bar you'd accept for saying a tool is "in-calibration" for a population (e.g., agreement with a set of known variants in that population, a minimum sample, a concordance level)? Or should everything stay provisional until we generate that evidence from the organoid arm?

**B2 · What makes a variant or target "ancestry-enriched"?**
The whole pipeline hinges on this phrase, and it can mean different things.
*What we need:* the operational definition you'd defend to a reviewer — a frequency **difference** between populations (how large?), a **continuous** correlation with AFR proportion (how strong?), and at what significance. And should this be judged **per population** (Ghanaian vs Ethiopian vs AA) or against continuous ancestry?

**B3 · What makes a target "actionable / druggable"?**
For nominating drug targets.
*What we need:* the evidence bar — e.g., a DGIdb interaction tier, an existing drug in development, inclusion in a clinical guideline, or something stricter. Where's the line between "interesting" and "actionable"?

**B4 · What counts as a meaningful differential drug response in the organoids?**
For the PDO screen comparing AA- vs NHW-derived organoids.
*What we need:* the endpoint that defines a real ancestry-linked difference — an IC50 fold-change threshold, an Emax difference, and the significance you'd require to call it a hit.

**B5 · What result would you accept as disconfirmation?**
We're building the system so it can report an honest null — *"the ancestry effect is smaller than the pilot suggested"* — rather than only finding what we expect.
*What we need:* what would that look like to you? (e.g., effect sizes below the Part-B2 threshold across the primary comparisons, or a specific pre-registered bar.) This protects the work from being a confirmation engine.

---

*Thank you — even partial answers unblock the corresponding pieces. Each item maps to a specific gate in the pipeline, so anything you confirm here directly turns on the next stage of analysis.*
